const friends = ['Mango', 'Kivi', 'Poly', 'Ajax', 'Andri', 'Liudmula'];
for (let i = 0; i < friends.length; i += 1 )
    console.log(friends[i]);


for (let friend of friends) {
    console.log(friend);
}